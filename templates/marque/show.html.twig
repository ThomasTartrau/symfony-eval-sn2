{% extends 'base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ marque.Nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ marque.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_marque_index') }}">retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('app_marque_edit', {'id': marque.id}) }}">Éditer</a>

    {# <a class="btn btn-primary" href="{{ path('app_abat_jour_new') }}">Créer une nouvelle abat-jour</a> #}

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
